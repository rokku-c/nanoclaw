apiVersion: apps/v1
kind: Deployment
metadata:
  name: scale-100-worker-004
  labels:
    app: scale-test
    scale: "100"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scale-100-worker-004
  template:
    metadata:
      labels:
        app: scale-100-worker-004
    spec:
      containers:
        - name: worker
          image: example.invalid/scale-worker:100-004
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
