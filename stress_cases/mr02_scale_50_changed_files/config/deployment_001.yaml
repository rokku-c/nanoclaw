apiVersion: apps/v1
kind: Deployment
metadata:
  name: scale-50-worker-001
  labels:
    app: scale-test
    scale: "50"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scale-50-worker-001
  template:
    metadata:
      labels:
        app: scale-50-worker-001
    spec:
      containers:
        - name: worker
          image: example.invalid/scale-worker:50-001
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
