apiVersion: apps/v1
kind: Deployment
metadata:
  name: scale-500-worker-030
  labels:
    app: scale-test
    scale: "500"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scale-500-worker-030
  template:
    metadata:
      labels:
        app: scale-500-worker-030
    spec:
      containers:
        - name: worker
          image: example.invalid/scale-worker:500-030
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
