# MR2 scale test: 500 changed files

This directory is a generated PR Review robustness payload.

Expected changed files when only `payload/` is copied into a repository: **500**.
Expected sentinel findings: **5**.

Use this payload to test whether the review agent can finalize structured results as PR size decreases from the original ultra-large PR.
