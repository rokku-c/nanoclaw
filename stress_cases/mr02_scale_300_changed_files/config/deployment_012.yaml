apiVersion: apps/v1
kind: Deployment
metadata:
  name: scale-300-worker-012
  labels:
    app: scale-test
    scale: "300"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scale-300-worker-012
  template:
    metadata:
      labels:
        app: scale-300-worker-012
    spec:
      containers:
        - name: worker
          image: example.invalid/scale-worker:300-012
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
