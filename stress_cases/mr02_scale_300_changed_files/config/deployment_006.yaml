apiVersion: apps/v1
kind: Deployment
metadata:
  name: scale-300-worker-006
  labels:
    app: scale-test
    scale: "300"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scale-300-worker-006
  template:
    metadata:
      labels:
        app: scale-300-worker-006
    spec:
      containers:
        - name: worker
          image: example.invalid/scale-worker:300-006
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
