apiVersion: apps/v1
kind: Deployment
metadata:
  name: scale-200-worker-002
  labels:
    app: scale-test
    scale: "200"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scale-200-worker-002
  template:
    metadata:
      labels:
        app: scale-200-worker-002
    spec:
      containers:
        - name: worker
          image: example.invalid/scale-worker:200-002
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
